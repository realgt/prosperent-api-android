package com.prosperent.api.model;

/***
 * The data returned by the API endpoint. The type of the data depends on the
 * endpoint (flat array, associative array, null, etc.). If an error occurs, the
 * data value should be ignored.
 * 
 */
public abstract class Data
{
}
